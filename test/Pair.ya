package Pair where

def Pair (A : *) (B : *) : * 
  = ∀.self
  (0 P : ∀ (_ : Pair A B) -> *)
  (& new : ∀ (a : A) (b : B) -> (P (λ P n => n a b)))
  -> P self

def new (0 A : *) (0 B : *) (a : A) (b : B) : Pair A B = λ P n => n a b
